int x = 50;
int y = 80;
int ecart = 10;
int longueur = 40;

size(200, 200);
background(255);
stroke(0);

// Pattes
line(x, y, x, y + longueur);

x += ecart;
line(x, y, x, y + longueur);

x += ecart;
line(x, y, x, y + longueur);

x += ecart;
line(x, y, x, y + longueur);

x += ecart;
line(x, y, x, y + longueur);

x += ecart;
line(x, y, x, y + longueur);

x += ecart;
line(x, y, x, y + longueur);

x += ecart;
line(x, y, x, y + longueur);

x += ecart;
line(x, y, x, y + longueur);

x += ecart;
line(x, y, x, y + longueur);

x += ecart;
line(x, y, x, y + longueur);